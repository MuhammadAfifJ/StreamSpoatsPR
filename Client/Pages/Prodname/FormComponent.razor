
<EditForm Model="@prodname" OnValidSubmit="@OnValidSubmit">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <fieldset disabled="@Disabled">
        <div class="form-group">
            <label>Product Name: </label>
            <div>
                <InputText @bind-Value="@prodname.ProductName" class="form-control" />
                <ValidationMessage For="@(() => prodname.ProductName)" />
            </div>
        </div>
    </fieldset>

    @if (!Disabled)
    {
        <button type="submit" class="btn btn-success btn-block">
            @ButtonText
        </button>
    }
    <a class="btn btn-outline-dark btn-block" href="/prodname/">
        <span class="oi oi-arrow-left"></span>
        Back To List
    </a>
    @*<BackToList Target="brand" />*@
</EditForm>

@code {
    [Parameter] public bool Disabled { get; set; } = false;
    [Parameter] public Prodname prodname { get; set; }
    [Parameter] public string ButtonText { get; set; } = "Save";
    [Parameter] public EventCallback OnValidSubmit { get; set; }
}
